{
  "$comment": "xotion-studio harness enforcement. The PreToolUse hook makes the 4-gate ship rule survive the LLM forgetting it: a rendered .mp4 may only reach a delivery destination via scripts/deliver.sh, which refuses unless qa-correctness + qa-richness + qa-audio + license-auditor all wrote status=pass.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "\"$CLAUDE_PROJECT_DIR/scripts/gate-guard.sh\"" }
        ]
      }
    ]
  }
}
